# Choose which model from Ultralytics library to use. 
# Use its filename, like 'yolo26n.pt', 'yolov5nu.pt' etc. Go to https://docs.ultralytics.com/models to see all models

model_filename = 'yolo26n.pt' 


# Or directly set path to .pt file of the model you installed.
# Takes priority over model_filename. Set it to '' if not used. 

path_to_model = '' 
